<beans:beans xmlns="http://www.springframework.org/schema/security"
    xmlns:beans="http://www.springframework.org/schema/beans"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.springframework.org/schema/beans
    http://www.springframework.org/schema/beans/spring-beans-4.0.xsd
    http://www.springframework.org/schema/security
    http://www.springframework.org/schema/security/spring-security-3.2.xsd">
 
 <!--  <beans:bean id="customAuthenticationProvider" class="my.spring.security.impl.CustomAuthenticationProvider"></beans:bean>
 
   <http auto-config="true" use-expressions="true">
        <intercept-url pattern="/user/admin**" access="hasRole('Admin')" />
        <form-login 
		    login-page="/user/login" 
		    default-target-url="/user/welcome" 
			authentication-failure-url="/user/login?error" 
			username-parameter="username"
			password-parameter="password" />
        <logout logout-url="/user/logout" />
    </http>
 
    <authentication-manager alias="authenticationManager">
        <authentication-provider ref="customAuthenticationProvider" />
    </authentication-manager>-->
    
    <!-- <beans:bean id="customAuthenticationProvider" class="my.spring.security.impl.CustomAuthenticationProvide">
		<constructor-arg name="userService" ref="userService" />
	</beans:bean> -->

	<http auto-config="true" use-expressions="true">
		<!-- <security:intercept-url pattern="/user/admin**" access="hasRole('Admin')" /> -->
		<intercept-url pattern="/user/**" access="hasRole('User')" />
		<form-login login-page="/login" default-target-url="/user/admin" authentication-failure-url="/login?error" username-parameter="username"
			password-parameter="password" />
		<logout logout-url="/logout" />
	</http>

	<authentication-manager alias="authenticationManager">
		<authentication-provider ref="customAuthenticationProvider" />
	</authentication-manager>

	<!-- <security:authentication-manager> <security:authentication-provider> <security:jdbc-user-service data-source-ref="dataSource" users-by-username-query="select 
		name,password,enabled from usertest where name=?" authorities-by-username-query="select u1.name, u2.name from usertest u1, role u2 where u1.id = u2.userId and u1.name 
		=?" /> </security:authentication-provider> </security:authentication-manager> -->
     
</beans:beans>